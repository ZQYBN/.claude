# Claude Code 全局规则

本文档是 AI 代理在所有项目中的行为约束。当项目级 `.claude/CLAUDE.md` 中的指令与本文档冲突时，以本文档为准。

---

## 一、通用层 — 所有项目永远适用

### 工作流

每次任务按以下阶段推进：

```
DISCUSS → PLAN → IMPLEMENT → TEST → REVIEW → DONE
```

#### DISCUSS

- 项目成员描述需求或报告缺陷
- AI 提问澄清，明确问题边界
- 功能需求与方案设计类讨论：调用 `brainstorming` skill 进行结构化需求探索
- 根本原因已明确且修改范围限于单一文件的缺陷：无需调用 `brainstorming`，直接进入问题定位
- 输出：对问题的一致理解
- AI 禁止在此阶段编写代码

#### PLAN

- AI 阅读相关模块代码，分析问题
- 根据任务规模选择计划形式：
  - **口头计划**：修改文件数少于 3 个且不涉及新模块创建的任务。在对话中口头说明根本原因、修复位置、修改方式，项目成员确认后直接执行。
  - **文本计划**：修改文件数达到 3 个及以上，或涉及新模块创建的任务。调用 `writing-plans` skill 辅助构建多步实施计划，写入项目的 `docs/superpowers/plans/<YYYY-MM-DD-slug>.md`。文本计划作为外部记忆，防止长对话中上下文丢失。
- PLAN 阶段不可跳过。AI 必须先说明要修改什么、如何修改。

#### IMPLEMENT

- 项目成员确认方案后，AI 创建 feature 分支，按提交逐步实现
- 涉及新建 UI 组件或页面时，调用 `frontend-design` skill
- 涉及前端设计决策（风格、配色、布局）时，调用 `ui-ux-pro-max` skill
- 提交前自检：`npx tsc --noEmit`（前端）/ `./mvnw.cmd compile`（后端）
- 实现完成后，更新关联文档（plan、spec 等）确保文档与代码一致。过期的文档与缺失的文档同样有害

#### TEST

- 提交完成后，调用 `MyTestBasedOnGit` skill 执行分层测试
- 测试基于 git diff 自动判定范围（前端/后端/数据库），仅运行与变更相关的测试
- 测试通过 → 进入 REVIEW
- 测试失败 → 修复后重新执行 TEST，不得进入 REVIEW

#### REVIEW

项目成员进行代码审查。AI 在审查前执行以下检查：

- 调用 `code-review-expert` skill 进行资深工程师视角审查
- 重构相关任务调用 `simplify` skill 检查复用性和效率
- 对照以下检查清单逐项自检：

**审查检查清单**：

- [ ] 功能符合 plan 中定义的目标和边界
- [ ] 无与任务无关的改动（diff 中每个变更都能说清目的）
- [ ] 未引入未授权的架构变化（新分层、新设计模式、目录重组等）
- [ ] 未暴露规则缺口（本次问题本应由某条规则防止，但该规则不存在）
- [ ] 无可沉淀的通用经验（同一模式出现 3 次以上时可考虑抽取，但不应提前抽象）
- [ ] 关联文档已更新（plan、spec 等与最终实现一致，不存在的接口或参数已修正）

若第 4 项（规则缺口）或第 5 项（可沉淀经验）被勾选，AI 在对话中向项目成员提出规则调整建议。项目成员确认后按本文档「规则管理」章节定义的流程执行。

#### DONE

- 项目成员手动创建 Pull Request 合并到 develop 分支，删除 feature 分支
- AI 主动询问是否需要归档 plan 文件（见下方归档规则）

---

### 分支规范

| 分支 | 职责 | 操作权限 |
|------|------|----------|
| `main` | 生产环境 | 项目负责人 |
| `develop` | 日常开发集成 | 全体项目成员 |
| `feature/xxx` | 新功能 | AI 创建，项目成员合并 |
| `bugfix/xxx` | 缺陷修复 | AI 创建，项目成员合并 |
| `release/x.x.x` | 发布前测试 | 项目负责人 |
| `hotfix/xxx` | 紧急修复 main 分支 | 项目负责人 |

**分支生命周期**：

```
develop
  └── feature/<name>   （AI 开发 → 项目成员 PR 合并 → 删除）
```

1. 项目成员确认方案后，从 develop 分支执行 `git checkout -b feature/<name>` 创建 feature 分支
2. AI 在 feature 分支按提交逐步实现
3. 实现完成后，执行 `git push origin feature/<name>` 推送到远程仓库
4. 项目成员手动创建 Pull Request 合并到 develop 分支
5. 合并完成后删除 feature 分支

feature 分支必须从 develop 分支创建。AI 禁止合并到 develop 分支，禁止操作 main 分支。

---

### Commit 规范

```
<type>(<scope>): <简短描述>
```

| type | 含义 |
|------|------|
| `feat` | 新功能 |
| `fix` | 缺陷修复 |
| `refactor` | 重构（功能不变） |
| `perf` | 性能优化 |
| `docs` | 文档更新 |
| `style` | 代码格式（不影响功能） |
| `test` | 测试 |
| `chore` | 构建/依赖配置 |
| `revert` | 撤回提交 |

- 简短描述不超过 50 个字符，结尾不加句号
- scope 字段使用英文小写字母（例如 `auth`、`api`、`prescription`）
- 每次提交只包含一项变更

---

### 文档归档

任务进入 DONE 阶段后，AI 主动询问是否需要归档 plan 文件。

**归档判断依据**：

| 文档类型 | 例子 | 归档时机 |
|----------|------|----------|
| 一次性任务 | 修复单个缺陷 | Pull Request 合并后归档 |
| 多阶段功能 | 分阶段执行的功能 | 全部阶段完成后归档 |
| 代码变更留痕 | 重构计划 | 项目成员决定保留或归档 |

**归档操作**：

- 项目成员确认归档后，执行 `git mv docs/superpowers/plans/<file> docs/archive/YYYY-MM/<file>`
- `specs/` 目录下的设计文档按相同规则处理
- AI 禁止在未获得项目成员确认的情况下自行归档

---

### 文档模板

**Plan 模板**（`docs/superpowers/plans/<YYYY-MM-DD-slug>.md`）：

```markdown
# <任务标题>

> 日期: YYYY-MM-DD
> 涉及领域: frontend / backend / both

## 背景
<为什么做这个改动>

## 目标与边界
- 目标：<具体要达成的结果>
- 非目标：<明确不做的>

## 涉及文件
- Modify: `<path>`
- Add: `<path>`

## 实现方案
<方案描述>

## 实施步骤
1. <第一步>
2. <第二步>

## 验收标准
- [ ] <可验证的条件>
```

**Spec 模板**（`docs/superpowers/specs/<YYYY-MM-DD-slug>.md`）：

```markdown
# <设计标题>

> 日期: YYYY-MM-DD

## 背景
## 目标与边界
## 架构
## 详细设计
## 风险与验证
```

Spec 是 Plan 的补充文档，用于需要详细技术设计的复杂功能。

---

### 编码原则

#### 实现前必须

1. 阅读项目 `docs/superpowers/plans/` 目录中的任务文件（如存在）
2. 阅读相关模块的现有代码
3. 确认数据库实际 schema（禁止仅依据 ORM 实体推断）
4. 输出实现计划（implementation plan），等待项目成员确认

AI 禁止在未完成方案设计并获得确认之前开始编写代码。

#### 裁决顺序

规则冲突时，按以下顺序决策（序号越小优先级越高）：

1. 数据安全 — 不丢失数据、不破坏数据
2. 正确性 — 行为正确优先于代码美观
3. 架构稳定性 — 不改动现有结构优先于优化结构
4. 可维护性 — 能被后续开发者理解
5. 最小影响范围 — 改动范围越小越好
6. 性能 — 仅在存在量化数据支撑时考虑
7. 代码风格 — 格式、命名等
8. 规则字面形式 — 以上均无法裁决时，服从字面更具体的规则

若仍无法判断，停止实现并请求项目成员确认。

#### 代码修改的优先级

1. 删除冗余代码
2. 合并重复逻辑
3. 减少不必要的文件数量（例如可合并的单一用途文件）
4. 最后才允许新增代码

#### 实现约束

- 不改变已有行为
- 不新增未被要求的功能
- 优先修改已有代码，而非新增代码
- 禁止过早抽象：同一模式出现 3 次以上重复后再抽取公共逻辑。禁止为"未来可能复用"而提前创建 `BaseXXX`、`AbstractXXX`、`useXXXManager`、`GenericXXXFactory` 等

---

### 范围控制

- 只修改当前任务相关的文件
- AI 禁止执行大范围重构
- AI 禁止修改与任务无关的模块
- AI 禁止执行"顺手优化"（与当前任务无直接关联的改进）
- AI 禁止修改公共基础设施（除非任务明确要求修改）

---

### 架构保护

除非任务明确要求，AI 禁止主动执行以下操作：

- 新增分层（例如在 service 层外增加 facade 层）
- 引入设计模式
- 修改状态管理结构
- 拆分或重组目录结构
- 调整领域模型边界
- 创建通用基础设施

发现架构问题时：先记录建议，等待项目成员确认后再修改。

---

### 注释规范

#### 文件头注释（必须）

每个文件必须以多行注释开头，说明文件职责与范围。新建文件必须包含，修改已有文件时若原注释已与代码不一致，须同步更新。

**前端**（TypeScript / JavaScript）：

```tsx
/**
 * <一句话描述文件职责>
 *
 * @remarks
 * - <关键设计决策或约束>
 * - <与其他模块的关系>
 */
```

**后端**（Java）：

```java
/**
 * <一句话描述类职责>
 *
 * <p>上下文：<在哪个模块中、服务于哪个功能></p>
 */
```

#### 函数与方法注释

- 公开 API、导出函数、Controller 方法、Service 方法：必须写注释
- 内部辅助函数与仅返回单一值的 getter 函数：可省略
- 仅返回 JSX 且不含业务逻辑的渲染函数：可省略
- 注释写"为什么"（Why），不写"做什么"（What，代码本身已经说明）

#### 注释禁止事项

- 不写 JSDoc 中无意义的 `@param` 与 `@returns`（例如 `@param id - the id`）
- 不在代码内写行注释解释"做什么"（例如 `// set loading to true`）
- 不写 TODO 注释（使用项目 `docs/todo/` 目录管理待办事项）

---

### 命令行使用规范

- 一次性低风险操作（文件重命名、文本搜索、单次替换等）：允许直接使用命令行
- 具有复用价值的批处理操作：优先复用项目 `script/` 目录中已有脚本；若无合适脚本，先评估是否值得沉淀为可复用脚本再创建
- 临时脚本仅在确实需要时创建，使用完毕后移入 `script/archive` 目录归档
- 禁止为一次性操作创建无复用价值的脚本

---

### 规则管理

#### 原则

规则的目标是：用过去的经验防止未来的错误。规则不应记录一切。

一条有效规则必须同时满足以下三项条件：

- 有明确的触发场景（何时适用）
- 有可追溯的来源（哪次事件或讨论促成了此规则）
- 有可验证的效果（遵守与违反的后果存在明确差异）

#### 规则写入的优先级选择

新增规则时，按以下优先级选择写入位置，序号越小越优先：

1. 修改现有局部规则 — 调整现有规则文件中的表述，而非新增独立条目
2. 调整任务模板 — 在 plan 或 spec 模板中增加检查项
3. 增强自动化检查 — linter、typecheck、测试框架能拦截的问题，不写入规则文件
4. 写入全局规则 — 仅跨任务、长期、重复发生的问题才写入本文档

**核心判断标准**：此问题下次是否会以相同形式再次出现？如果不会出现，不写入规则。

#### 规则触发条件

以下情况应新增或修改规则：

| 触发条件 | 典型场景 |
|------|------|
| 重复犯错 | 相同类型的缺陷出现 2 次及以上 |
| 单次重大事故 | 数据丢失、生产环境故障、安全漏洞 |
| 发现规则冲突 | 两条规则无法同时遵守 |
| 规则过时 | 技术栈升级、项目阶段变更导致规则不再适用 |
| 规则过度约束 | 规则导致 AI 做出明显较差的选择 |

以下情况不应新增规则：

- 一次性小失误（不值得规则化）
- 仅在特定上下文有效的经验（通用性不足）
- 可以被 linter、formatter 或自动化工具替代的检查项
- 纯主观偏好（缺乏可验证标准的个人审美判断）

#### 规则生命周期

每条规则处于以下三种状态之一：

| 状态 | 含义 | 标记方式 |
|------|------|----------|
| Active | 当前有效，必须遵守 | 无标记（默认状态） |
| Deprecated | 即将废弃，仍可参考但不强制执行 | 规则文本前加 `[DEPRECATED]` 标记 |
| Archived | 已失效，仅保留历史记录 | 直接删除（规则文件体积小，无需保留历史文本） |

触发状态变更的条件：

- 技术栈大版本升级后，重新评估相关规则是否仍然适用
- 项目阶段变更（例如从 demo 阶段进入生产阶段），重新评估约束强度
- 发现规则长期未被触发（失效规则），标记为 Deprecated，项目成员确认后删除

#### 变更流程

1. 发现 — 项目成员在对话中指出问题，或 AI 主动识别到规则不足
2. 讨论 — 确认此问题属于规则层面的缺失，还是单次执行失误
3. 草拟 — AI 起草规则文本
4. 确认 — 项目成员审核批准后，AI 写入对应规则文件
5. 记录 — 在提交信息（commit message）中注明规则变更来源

```
chore(rules): 新增 xxx 规则

来源:
- 日期: YYYY-MM-DD
- 类型: 重复问题 / 单次事故 / 规则冲突 / 过时清理
- 事件: <简述>
```

#### 编写规范

每条规则必须包含以下三个要素：

- 行为描述（应当做什么、不应当做什么）
- 动机说明（用一句话解释为什么需要此约束）
- 触发条件（何时适用此规则）

正面示例：

> "禁止为未来可能复用而提前抽象。同一模式出现 3 次以上重复后再抽取公共逻辑。"
>
> 来源:
> - 日期: 2026-05
> - 类型: 重复问题
> - 事件: AI 多次创建 BaseXXX、AbstractXXX 等空壳抽象，增加维护成本

#### 修改权限与反面模式

AI 禁止在未获得项目成员确认的情况下修改任何规则文件。

反面模式：

- 过度特定化规则：在单一事件后制定极其具体的规则。正确做法是抽象到原则层面。
- 规则膨胀：每次对话新增一条规则。优先修改现有规则，而非新增。
- 失效规则：规则已写入但长期未被触发。标记为 Deprecated 后清理。
- 冲突规则：两条规则指向相反方向。按裁决顺序裁决，或合并为一条。

---

### Skill 管理

Skill 是 AI 的专用能力模块，每个 Skill 封装特定领域的知识和执行流程。管理目标：高频场景自动触发，低频场景手动调用。

#### Skill 目录

**需求与设计**：

| Skill | 用途 | 触发 |
|-------|------|------|
| `brainstorming` | 需求探索、意图澄清、方案设计 | 自动：DISCUSS 阶段 |
| `writing-plans` | 将 spec/需求转为多步实施计划 | 自动：复杂功能 PLAN 时 |
| `frontend-design` | 高质量前端界面设计 | 自动：新建 UI 组件/页面 |
| `ui-ux-pro-max` | UI/UX 设计智能（50+ 风格、161 调色板、57 字体配对） | 自动：前端设计决策 |

**代码质量**：

| Skill | 用途 | 触发 |
|-------|------|------|
| `code-review-expert` | 资深工程师视角的代码审查 | 自动：REVIEW 阶段 |
| `simplify` | 审查代码复用性、质量、效率并修复 | 自动：重构类任务 REVIEW |
| `MyTestBasedOnGit` | git diff 驱动分层测试（前端/后端/数据库自动判定） | 自动：TEST 阶段 |
| `review` | Pull Request 审查 | 手动（项目成员发起 Pull Request 前） |
| `security-review` | 安全审查 | 手动（涉及认证、数据、API 变更时） |

**图表与可视化**：

| Skill | 用途 | 触发 |
|-------|------|------|
| `meimaid-skill` | Mermaid 图生成并导出 PNG/SVG/PDF | 手动（需要绘图时） |
| `mermaid-diagrams` | Mermaid 语法指南（11+ 图类型） | 手动（需要绘图时） |

**工具与配置**：

| Skill | 用途 | 触发 |
|-------|------|------|
| `skill-creator` | 创建、优化、评估 skills | 手动（需要定制新 skill 时） |
| `find-skills` | 发现和安装新 skills | 手动（需要新能力时） |
| `update-config` | 配置 Claude Code settings.json | 手动（配置变更时） |
| `keybindings-help` | 自定义键盘快捷键 | 手动（快捷键配置时） |
| `fewer-permission-prompts` | 扫描转录并优化权限白名单 | 手动（权限提示过多时） |
| `loop` | 定时循环执行命令 | 手动（轮询需求时） |
| `claude-api` | Anthropic SDK 与 API 开发调试 | 手动（涉及 Anthropic SDK 时） |
| `init` | 初始化新项目的 CLAUDE.md 文件 | 手动（新项目启动时） |

#### 自动触发详情

| Skill | 触发时机 |
|-------|----------|
| `brainstorming` | DISCUSS：功能需求或方案设计 |
| `writing-plans` | PLAN：复杂功能需要多步计划 |
| `code-review-expert` | REVIEW：提交前资深工程师视角审查 |
| `simplify` | REVIEW：重构后检查复用性和效率 |
| `frontend-design` | IMPLEMENT：新建 UI 组件或页面 |
| `ui-ux-pro-max` | IMPLEMENT：前端设计决策（风格、配色、布局） |
| `MyTestBasedOnGit` | TEST：git diff 驱动分层测试 |

#### Skill 变更规则

| 操作 | 触发条件 | 流程 |
|------|----------|------|
| 提升为自动触发 | 同一 skill 被手动调用 3 次及以上 | AI 提议，项目成员确认 |
| 降级为手动触发 | skill 自动触发后常被跳过或产出不符合预期 | AI 提议移除自动触发，项目成员确认 |
| 删除 | skill 无法达到预期效果、产出质量未达到可接受标准 | 项目成员直接指令删除，AI 清除所有引用 |
| 新增 | 安装新 skill | 手动调用并验证有效后，按提升流程处理 |

- AI 禁止自行增删 skills，禁止修改自动触发配置
- 删除 skill 时，AI 必须扫描所有规则文件，确保无残留引用

Skills 和 Rules 互补：

- Rule 约束 AI 能做什么、不能做什么（定义边界）
- Skill 赋予 AI 特定领域能力（提供工具）
- Rule 决定何时调用哪个 Skill（定义触发条件）

---

## 二、技术栈层 — 条件适用

以下规则在项目使用对应技术栈时自动生效。

### 前端：React + TypeScript + Ant Design 6

#### Skills

前端任务自动调用以下 skills：

- 新建 UI 组件或页面 → `frontend-design`
- 设计决策（风格、配色、布局）→ `ui-ux-pro-max`

#### React

- 必须使用函数组件与 Hooks，禁止使用类组件（class component）
- 禁止使用 TypeScript `any` 类型，所有类型必须明确声明
- 禁止页面宽高抖动：Select 与 Input 组件须使用固定宽度，不得因选中内容长度变化导致布局偏移；Table 列宽须明确指定

#### Ant Design 6

- 禁止使用已废弃的 API
- Notification 组件禁止使用 `message` 字段，统一使用 `title` 字段
- Drawer 组件禁止使用 `width` 与 `destroyOnClose` 属性，统一使用 `size` 与 `destroyOnHidden` 属性
- 禁止新增 `antd` 的 `List` 组件用法，优先使用原生语义容器元素

#### 大规模树结构

必须满足以下要求：

- 使用虚拟滚动
- 使用扁平投影（flat projection）
- 使用稳定且唯一的 key
- 展开状态操作的时间复杂度为 O(1)

禁止以下行为：

- 深层递归渲染
- 每次渲染时重复创建内联函数（应使用 useCallback 或提取为组件外部函数）
- 每次渲染时重新构建树形数据结构（应在 useMemo 中或仅在数据变更时构建）

---

### 后端：Spring Boot 3 + JPA + MySQL

#### API 规范

- 所有接口返回值必须包装在统一结构 `{ code, message, data }` 中
- 禁止直接返回实体对象或原始类型（即禁止返回裸数据）

#### 错误处理

- 禁止捕获异常后不进行任何处理（包括：不记录日志、不向上层抛出、不返回错误响应）。禁止使用空的 catch 块
- 每条错误日志必须包含：发生时间、触发操作的上下文、异常堆栈。日志级别须与实际严重程度一致

#### 数据库

- 实现前必须确认数据库实际 schema（包括：字段是否可为空、索引定义、状态字段的合法取值）
- 禁止仅依据 ORM 实体类推断数据结构
- 禁止自动执行破坏性 SQL 语句（DROP TABLE、TRUNCATE、无 WHERE 条件的 DELETE 等）

---

### 测试规则

IMPLEMENT 阶段提交后，由 TEST 阶段统一执行分层测试（`MyTestBasedOnGit` skill）。

提交前本地自检（可选，用于快速验证编译通过）：

- 前端：`npx tsc --noEmit`
- 后端：`./mvnw.cmd compile`

正式测试由 TEST 阶段执行，不在此处重复定义。
